/**
 * Re-export da versão canônica em `@portal/core/http/bffError` — dedup de uma
 * implementação que existia idêntica aqui e em `mapa-salas/_lib/bffError.ts`.
 */
export { bffErrorResponse } from "@portal/core/http/bffError";
