/**
 * Gap da grade de assentos, compartilhado entre a grade real (`MapGrid`) e o
 * skeleton (`MapGridSkeleton`) para que fiquem alinhados no swap — nenhum dos
 * dois define gap próprio (dívida documentada nos dois componentes). Aproximação
 * do respiro do Figma; ajustável num follow-up de fidelidade visual.
 */
export const MAP_GRID_GAP = 'gap-x-8 gap-y-6'
